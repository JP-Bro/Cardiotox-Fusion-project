# Cardiotox-Fusion — Python Dependencies
# =========================================
# Install with: pip install -r requirements.txt
#
# IMPORTANT: Install PyTorch BEFORE torch_geometric.
# For CPU-only (Windows without NVIDIA GPU):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
#
# For CUDA 11.8:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# For CUDA 12.1:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# After PyTorch, install torch_geometric:
#   pip install torch_geometric
#
# All other dependencies below can be installed directly.

# --- Core data science ---
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.11.0

# --- Chemistry ---
rdkit>=2023.9.1

# --- Deep learning (install PyTorch manually first, see above) ---
# torch  -- install manually (see above)
# torch_geometric  -- install manually after torch

# --- Data reading ---
openpyxl>=3.1.0         # required by pandas for .xlsx files
requests>=2.31.0         # ChEMBL API calls

# --- LINCS expression matrix parsing (optional, needed for Step 3 --extract) ---
# cmapPy>=4.0.1         # uncomment when ready to parse GCTX files
#                        # install: pip install cmapPy

# --- Visualization ---
matplotlib>=3.7.0
seaborn>=0.12.0
Pillow>=9.0.0            # for RDKit molecule image generation

# --- Jupyter (for notebook work) ---
jupyter>=1.0.0
notebook>=7.0.0
ipykernel>=6.25.0

# --- Progress & logging ---
tqdm>=4.65.0

# --- Development ---
pytest>=7.4.0            # for running tests (scripts/tests/)
